rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Server-side only via Admin SDK — no client reads/writes.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
