Branch 2 — feature/signal-intelligence
Language + analytics on the data you already have.

Caption/comment classification upgrades (keywords → labeled set → simple NLP/model pass)
Reprocess pipelines (process_*) writing richer categories/confidence
Analytics cleanup: source pages, filters, “missed story” views driven by new fields
No auth/DB blockers — work against JSON now, swap to DB reads when platform lands